// TODO: Club and also the quick object model
export interface Club {
  admins: string[]
  description: string
  joinPreference: number
  members: string[]
  name: string
  pending: string[]
  pictureId: string
}
